ktlint {
    version "$ktlint_version"
    debug = true
    verbose = true
    android = true
    reporters {
        reporter "plain"
        reporter "checkstyle"

    }
    ignoreFailures = false
}
